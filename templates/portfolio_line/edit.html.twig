{{ portfolio_line.id }}
{{ form_start(form, {'action': path('portfolio_line_edit', { 'id': portfolio_line.id })}) }}
    {{ form_widget(form) }}
    <button class="button is-block is-primary">
        {{ button_label|default('Enregistrer') }}
    </button>
{{ form_end(form) }}