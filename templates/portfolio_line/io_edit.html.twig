{{ form_start(form, {'action': path('portfolio_line_io_edit', { 'id': portfolio_line.id })}) }}

<p>{{ portfolio_line.fund.isin }} - {{ portfolio_line.fund.name }}</p>
<p>Valeur arbitrée actuelle : {{ portfolio_line.ioValue }} €</p>

<div class="field">
    <label class="label">{{ form_label(form.ioValue) }}</label>
    <div class="control">
        {{ form_widget(form.ioValue, { 'attr': {'class': 'input is-medium'} }) }}
    </div>
</div>

<div class="control">
    <button class="button is-block is-primary">
        {{ button_label|default('Enregistrer') }}
    </button>
</div>

{{ form_end(form) }}