<form method="post" action="{{ path('portfolio_io_delete', {'id': portfolio_io.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ portfolio_io.id) }}">
    <button class="btn">Delete</button>
</form>
